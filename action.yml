name: 'TODO Finder Action'

description: 'Find and comment on TODOs in pull requests'

inputs:
  token:
    description: 'GitHub token'
    required: true

runs:
  using: 'node20'
  main: 'dist/index.js'
